# FAD
Open source fadderportal som möjligör lätt hanteringa av fadderjobb och planering av mottagningsperiod vid liu

- naviga till FAD

- kör `docker compose up` för att starta projektet

- navigera till `localhost:3000` för att se frontend

- navigera till `localhost:8000` för att se backend