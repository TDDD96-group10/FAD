services:
  test-django:
    image:  ghcr.io/${IMAGE_OWNER}/my-django-app:latest
    command: >
      bash -c "coverage run manage.py test && coverage report --fail-under=80"
    volumes:
      - ./fad_backend:/app


